export default function Instructions() {
    return <p>
        If Docker Compose Watch was used, try editing <code>src/App.tsx</code>'s
        return or change which "swap" asset is imported, then save the file to
        see Compose Watch and Vite Hot Module Replacement in action.
    </p>
}